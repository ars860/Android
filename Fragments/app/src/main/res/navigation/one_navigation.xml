<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/one_navigation"
    app:startDestination="@+id/buttonFragment1">
    <fragment
        android:id="@+id/buttonFragment1"
        android:name="com.example.fragments.fragments.ButtonFragment"
        android:label="fragment_button1"
        tools:layout="@layout/fragment_button">
        <argument
            android:name="number"
            app:argType="integer" />
        <action
            android:id="@+id/action_buttonFragment_self"
            app:destination="@id/buttonFragment1" />
    </fragment>
</navigation>